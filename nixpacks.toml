# Nixpacks configuration for FlipOps Site
# Tells Railway how to build and run Next.js with proper static asset serving

[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["npm ci --include=dev"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run start"
