# Force Railway cache clear - use standard Next.js build
[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["npm ci --include=dev"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "bash scripts/railway-start.sh"
